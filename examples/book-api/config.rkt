'(
    (middlewares (list aaa bbb ccc))
    
    (app-port 8000)
    (log-file "app.log")
    
    (databases (hash 
        'mysql (hash 'user "root" 'password "123")
        'mongodb (hash 'user "root" 'password "123")
    ))
)


(let* 
    ([log-file "app.log"]
    [database (
        let 
    )]
    [log-file "app.log"]
    )
    



)